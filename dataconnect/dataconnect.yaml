specVersion: "v1"
serviceId: "birthday-countdown"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "birthday-countdown-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
